#!/bin/bash
sudo apt-get update -y
sudo apt-get upgrade -y
sudo apt-get install apache2 -y
echo "loan balancer arn: ${jsonitem['LoadBalancers'][0]['LoadBalancerArn']}"
echo "target group arn: ${jsonitem1['TargetGroups'][0]['TargetGroupArn']}"
